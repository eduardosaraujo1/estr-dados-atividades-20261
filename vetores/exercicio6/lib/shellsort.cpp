void insertion_sort_gap(int *arr, int len, int gap) {
  // Executar insertion sort em todos os subarrays com espaço definido
}

void shellsort(int *arr, int len) {
  // Chamar método para todos gaps do shellsort (algoritmo a ser definido)
}